# 从「SoX 参数拉满」再往上走：PCM → DSD 的优化路线与开源工具链

前提：你已经会用

```
sox_ng in.flac out.dsf rate -u 5644800 sdm -f sdm-6 -t 8 -n 16 -l 512
```

本文回答的是「参数拉满之后还能优化什么、怎么做、用什么开源工具」。
所有结论尽量标明是**源码/manpage 确证**还是**待实测**。

---

## 1. 先认清 SoX 调制器的天花板在哪

`src/sdm.c` 读完之后的结论：

| 维度 | SoX 提供的能力 | 可优化空间 |
|---|---|---|
| 噪声整形滤波器 | **硬编码 18 个**：`clans-*`/`sdm-*` × 阶数 4–8，且每个滤波器绑定速率（仅 DSD64/128/256 三档） | **零**。不能自定义 NTF，没有 DSD512 档 |
| 调制器拓扑 | 单一结构（`sdm_filter_calc`，单环 + `g[]` 谐振子反馈） | **零**。换拓扑必须换程序 |
| Trellis | order 3–32、paths 4–32、latency 100–2048 | 有，且**边际收益递减** |
| 输入电平 | `x = sample * 0.5`，0 dBFS = 满幅 ±1 | **有，且很关键**（见 §3） |
| 目标速率 | 由外部 `rate` 决定 | **有**：DSD128/256 本身就换更温和的整形 |

**因此：「进一步优化」不可能靠在 SoX 内部再叠一级噪声整形。**
真正的杠杆只有四个：

1. **测量与调参闭环**（目前最缺）
2. **目标速率 + 滤波器阶数的重新选点**
3. **输入级处理**（真峰/增益/限幅策略）
4. **离开 SoX 自己写调制器**（上限最高，成本也最高）

---

## 2. 优先级 P0：建立可信测量台（最大的实际收益）

没有测量，前面所有参数都是在盲调。SoX 的 `stats` 只给 RMS/峰值，
**给不了带内噪声底、空闲音、THD**——这些必须自己算。

### 2.1 测量链路的正确形态

```
DSD (.dsf/.dff)
  → 解回 PCM（必须用「正确」的 DSD 抽取滤波器）
  → 限带到 20 Hz–20 kHz
  → FFT / 加权 RMS
```

三个必须做对的地方：

- **抽取滤波器要用真正的 DSD 低通**，不要用普通重采样。
  SoX 的 `rate`、以及 `sinc` 都能用，但**测之前要确认它在 20 kHz 处的通带平坦度**，
  否则你量到的是测量工具的滚降，而不是调制器的噪声底。
- **限带后再算 RMS/FFT**：DSD64 在 20 kHz 以上的噪声比带内高 40 dB 以上，
  不限带得到的数字毫无意义。
- **加权**：裸 RMS 会被 15–20 kHz 的整形噪声主导，与听感不符。
  建议同时报「20 kHz 限带 RMS」与「A 加权 RMS」两个数。

### 2.2 工具（全部开源）

| 工具 | 用途 | 说明 |
|---|---|---|
| **`dsd2pcm`**（mansr） | DSD → PCM 的专业抽取 | 与 SoX 的 `sdm` 同一作者，测量链首选 |
| **`sox_ng`** | 编解码、重采样、限带、`stats` | 你现在用的 |
| **NumPy / SciPy**（Python） | FFT、加权、THD、空闲音搜索、批量扫描 | 唯一的「分析大脑」 |
| **GNU Octave** | 同上，若不想用 Python | 可跑 Schreier 的 .m 脚本 |
| **REW (Room EQ Wizard)** | 交互式频谱、THD+N、看空闲音 | Java 开源，免费，图形化 |
| **Audacity** | 快速看频谱、导出为 DSD 做参考对照 | GPL |

> 实践建议：**用 Python 写一个扫描器**，把「参数组合 → 编码 → 解码 → 指标」
> 全自动化。参数空间不小（滤波器 18 档 × trellis 若干 × 速率 3 档 × 增益若干），
> 手工跑必然漏掉最优解。
>
> 仓库里已有的 `verify-dsd.ps1` 是这条链路的简化版（静音/低电平/参考电平三探针），
> 可以先跑它拿到基线，再用 Python 做大规模扫描。

### 2.3 要量哪些指标（按重要性）

1. **静音输入的带内噪声底**——决定「背景有多黑」
2. **空闲音（idle tone）**：静音 + −60 dBFS 输入下，带内有无离散尖峰
   ——DSD 最典型的听感缺陷就是它，比噪声底更容易被听出来
3. **THD**：0 dBDSD（PCM −6 dBFS）1 kHz 正弦的谐波
4. **过载门限扫描**：从 −20 dBFS 起以 0.5 dB 步进升到 0 dBFS，
   找出「带内噪声突然抬升」的拐点，这就是**你这套参数的真实峰值上限**
   ——它可能比 SACD 规范的 +3.1 dBDSD 更低，必须自己量出来
5. **收敛失败计数**：`sdm` 收尾时若打印 `failed to converge N times`，
   说明 trellis 候选路径塌缩，该参数的测量结果不可信

---

## 3. 优先级 P1：输入级处理（几乎没人做，但收益确定）

源码里 `x = sample * 0.5`，这条式子意味着：

- **PCM 0 dBFS → 调制器 ±1.0**，而调制器稳定上限约 ±0.71 → **0 dBFS 素材必然过载**
- 你的素材峰值若在 −0.1 dBFS，实际有约 **+5.9 dB** 的超限
- 反过来，如果素材是「24-bit 但峰值 −6 dBFS」，则白白损失 6 dB 的 SNR

### 3.1 该做的工作

1. **真峰值（ISP）检查**：播放链路上的 intersample peak 可能比样本峰值高 1–3 dB。
   SoX 侧可用 `rate` 升采样后再 `stats` 观察；更严谨用 Python + `scipy.signal.resample_poly`。
2. **逐曲定增益**，而不是全局 `gain -6`：
   目标是让「最坏情况的真峰」落在你实测出的过载门限之下（留 0.5–1 dB 余量）。
3. **慎用软限幅**：给 DSD 做 PCM 域限幅会把失真留在带内，
   而调制器过载产生的是带内噪声抬升 + 空闲音。两者听感不同，需要 A/B 决定。
   更干净的做法是**整体降增益**而不是限幅。
4. **DC 处理**：`stats` 看 DC offset，非零先 `highpass 10`。
   DC 会持续占掉调制器裕量并移动空闲音频率。
5. **不要加整形 dither**：`sdm` 自带整形。降位时用 TPDF（`dither`）即可。

---

## 4. 优先级 P2：重新选点（速率 × 阶数）

这是「不改程序也能拿到大收益」的一档。

- 源码滤波器表只有 **DSD64 / 128 / 256** 三档，**每个速率下 4–8 阶各一套系数**。
- 速率翻倍 → 量化噪声被整形到更宽的频带、带内残留更低 → 同等带内噪声下
  整形可以更温和、更稳定。
  （注意：噪声整形每倍频约 +6 dB 是**带外**斜率；带内噪声的下降幅度取决于阶数与
  积分带宽，不是简单的每倍频 6 dB，必须实测。）
- 社区推荐（wiki，需实测复核）：DSD64 → clans-8；DSD128 → clans-7；DSD256 → clans-6。

### 该做的实测

对 **DSD128 与 DSD256**，各把 4/5/6/7/8 阶都跑一遍（`clans-*` 与 `sdm-*` 都跑），
量 §2.3 的五个指标，画出「阶数 vs 带内噪声底 vs 过载门限」的表。

预期结论通常是：**DSD256 + 中阶** 在带内噪声、稳定性、算力三者上同时优于
**DSD128 + 高阶**——但这个结论必须由你自己的测量得出，不要照抄。

代价提醒：速率翻倍，`sdm` 的算力基本翻倍，trellis 的开销还会随路径数增长。
先用 10 秒素材测实时倍率。

---

## 5. 优先级 P3：离开 SoX，自己写调制器（上限最高）

只有当 §2–§4 做完、确认瓶颈确实在「滤波器不能自定义」时，才值得走这一步。

### 5.1 拆成两段

```
PCM →（升采样，仍用 sox_ng rate -u）→ 中间高采样率 PCM →（你自己的调制器）→ 1-bit 流 → 封装
```

封装环节：`.dsf` / `.dff` 的头部结构简单，可用 Python 直接写；
或输出裸 1-bit 再由 sox_ng 包成容器。

### 5.2 开源工具

| 工具 | 用途 |
|---|---|
| **`python-deltasigma`**（PyPI `deltasigma`） | Schreier Delta Sigma Toolbox 的 Python 移植：合成 NTF/STF、`simulateDSM` 仿真、SNR 分析、拓扑映射（CRFB/CRFF 等）。**设计新调制器的首选** |
| **Schreier Delta Sigma Toolbox**（MATLAB/Octave） | 同上，原版 .m；配合 Octave 完全开源可用 |
| **Reefman & Janssen 2002 教程**（sdm.c 引用的第一参考文献） | DSD 用 ΣΔ 调制的入门与系数设计方法，理解 `a[]`/`g[]` 从哪来 |
| **Harpe 2003「Trellis-type Sigma Delta Modulators for SACD」**（第二参考文献） | trellis/Viterbi 搜索的理论与增益，判断「加大 order/num 还值不值」 |

> sdm.c 的注释里明确列了这三篇参考文献。**要超越它，先读它读的东西。**

### 5.3 自己写调制器时，真正可调的是什么

- **NTF 的带外增益（H_inf）**：不能无限大，通常压在 1.5–2.0 之间换取稳定；
  这是「噪声底 ↔ 过载门限」的核心旋钮
- **极点位置**：决定带内噪声的形状（是否把噪声尽量推离 20 kHz 以下）
- **拓扑**（CRFB/CRFF/CIFB…）与**系数量化**：影响实际可实现性
- **限幅/复位策略**：过载时如何恢复，决定崩溃后的听感

### 5.4 一条现实的性价比判断

自己写调制器能拿到的增益，量级上通常来自：
「更高的有效阶数」或「更激进的带外增益」——**两者都以稳定性和过载门限为代价**。
如果你的素材峰值已经在 −6 dBFS 附近、且你愿意用 DSD256，
那么 **在 SoX 内重选阶数 + 把输入电平管好** 可能已经拿到九成收益，
而自写调制器的边际收益只剩最后一成，却要承担全部正确性风险。

**先做 §2 的测量，用数据决定要不要走 §5。**

---

## 6. 不要做的事

1. **不要在 `sdm` 之后再叠一级噪声整形/dither**：整形已在调制器内完成（`cost` 用 `a[0]` 加权），
   再叠只会抬高带内噪声。
2. **不要对 DSD 做 PCM 域的重采样来「降噪」**：超声噪声属于 DSD 的固有结构，
   想控它应该调 NTF 或提高速率，而不是事后处理。
3. **不要直接用 DSD512**：滤波器的匹配规则是「**freq ≤ 目标速率**的第一个表项」，
   表里最高档只到 `256*44100`，所以在 512×44100 下会**沿用为 DSD256 设计的系数**，
   整形的零极点位置与目标速率不匹配。此时正确的做法不是照抄参数，
   而是先仿真确认为该速率设计的 NTF（或干脆停在 DSD256）。
4. **不要只看 `stats` 的峰值就说「没削波」**：DSD 的过载表现为带内噪声抬升与空闲音，
   PCM 域的峰值看不出来。
5. **不要忽略 stderr 的 `failed to converge` 警告**：那意味着 trellis 已经不在正常工作。

---

## 7. 建议的执行顺序

```
① 拿到 DSD 版 sox（sox_ng 14.8.1 win64 预编译包）
② 跑 verify-dsd.ps1 建立基线（静音噪声底 / −60 dBFS 空闲音 / −6 dBFS 参考）
③ 用 Python 写参数扫描器：滤波器 × 阶数 × 速率 × 增益 → 五指标
   优先扫：DSD128 vs DSD256、clans-* vs sdm-*、阶数 4–8
④ 用 ③ 的结果定「逐曲增益 + 过载余量」
⑤ 若 ③ 显示瓶颈在「滤波器不能自定义」→ 用 python-deltasigma 设计自己的 NTF
   先仿真（simulateDSM）确认 SNR 与稳定性，再落成实现
⑥ 全程保留测试矩阵与参考命令，任何改动都能回溯
```
